services:
  - type: web
    name: rollobtc-api
    runtime: python
    plan: free
    buildCommand: pip install -r api/requirements.txt
    startCommand: python api/server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 10000
